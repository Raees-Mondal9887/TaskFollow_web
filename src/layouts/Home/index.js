import React from 'react';
import { Button } from 'rsuite';

const Home = () => {
  return (
    <main>
      <Button appearance="primary">Hello World</Button>
    </main>
  );
};

export default Home;
